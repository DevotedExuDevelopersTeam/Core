version: "3.8"
services:
  main:
    build: .
    container_name: core
    network_mode: host
    volumes:
      - /home/exenifix/core-data/backgrounds:/app/backgrounds
    environment:
      - TOKEN
      - DATABASE=core
      - PASSWORD
      - USER=core
      - GOOGLE_API_KEY
